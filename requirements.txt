# BLDC PHM: Python package dependencies.
# Install on a new machine with:  pip install -r requirements.txt
pyserial>=3.5
PyQt5>=5.15
numpy>=1.24
pandas>=2.0
openpyxl>=3.1        # writing the UNMOUNTED.xlsx / MOUNTED.xlsx workbooks
scipy>=1.10          # logo background removal; robust stats
PyYAML>=6.0
Pillow>=10.0         # app logo install (tools/set_logo.py)
# pyqtgraph no longer required (Monitor tab uses a lightweight value display)

# Needed ONLY when you add the RS-485 / Modbus telemetry (MAX485 module):
# pymodbus>=3.5
